{{ header }}
<section id="account-edit" class="register update-profile" style="background: #F3F3F3;">
    <div class="container">
        <ul class="breadcrumb">
            {% for breadcrumb in breadcrumbs %}
                <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
            {% endfor %}
        </ul>
        <div class="row">
            {{ profile_column_left }}
            <div class="col-12 col-md-9 col-lg-9">
                <div class="pb-5">
                    <div class="form-signin card py-3 px-2">
                        <div id="account-edit" class="container">
                            {% if error_warning %}
                                <div class="alert alert-danger alert-dismissible"><i
                                            class="fa fa-exclamation-circle"></i> {{ error_warning }}</div>
                            {% endif %}
                            <div class="row">{{ column_left }}
                                {% if column_left and column_right %}
                                    {% set class = 'col-sm-6' %}
                                {% elseif column_left or column_right %}
                                    {% set class = 'col-sm-9' %}
                                {% else %}
                                    {% set class = 'col-sm-12' %}
                                {% endif %}
                                <div id="content" class="{{ class }}">{{ content_top }}
                                    <div class="justify-content-between row mx-0 align-items-center">
<div>
                                    <h4 class="f-20 font-weight-bold text-uppercase mb-0">{{ heading_title }}</h4>
</div>
                                        <div>
                                    {% if role == 'delivery-partner' %}
                                        <a href="{{ delivery_link }}" class="btn btn-success ">Update Delivery Partner Info</a>
                                    {% endif %}
                                        </div>
                                    </div>
                                    <hr>
                                    <form action="{{ action }}" method="post" enctype="multipart/form-data"
                                          class="form-horizontal">
                                        <fieldset class="">
                                            <div class="row">
                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label"
                                                           for="input-firstname">{{ entry_firstname }} <span
                                                                class="text-danger">*</span> </label>
                                                    <div class="">
                                                        <input type="text" name="firstname" value="{{ firstname }}"
                                                               placeholder="{{ entry_firstname }}" id="input-firstname"
                                                               class="form-control rounded-0"/>
                                                        {% if error_firstname %}
                                                            <div class="text-danger">{{ error_firstname }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label"
                                                           for="input-lastname">{{ entry_lastname }}<span
                                                                class="text-danger">*</span> </label>
                                                    <div class="">
                                                        <input type="text" name="lastname" value="{{ lastname }}"
                                                               placeholder="{{ entry_lastname }}" id="input-lastname"
                                                               class="form-control rounded-0"/>
                                                        {% if error_lastname %}
                                                            <div class="text-danger">{{ error_lastname }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label"
                                                           for="input-date">{{ entry_date_of_birth }}<span
                                                                class="text-danger"> *</span> </label>
                                                    <div class="">
                                                        <input type="date" name="date_of_birth"
                                                               value="{{ date_of_birth }}"
                                                               placeholder="{{ entry_date_of_birth }}" id="input-date"
                                                               class="form-control rounded-0"/>
                                                        {% if error_date_of_birth %}
                                                            <div class="text-danger">{{ error_date_of_birth }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>

                                                <div class="col-md-6 form-group">
                                                    <label for="inputGender" class="d-block">Your Gender</label>

                                                    <input type="radio" name="gender" value="male"
                                                           class="icheck-input" {% if gender == 'male' %} checked {% endif %}>
                                                    <label class="mr-5">Male</label>

                                                    <input type="radio" name="gender" value="female"
                                                           class="icheck-input" {% if gender == 'female' %} checked {% endif %}>
                                                    <label>Female</label>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label"
                                                           for="input-date">{{ entry_home_address }}<span
                                                                class="text-danger">*</span>

                                                    </label>
                                                    <div class="d-inline-block float-right"><small><a
                                                                    href="{{ manage_address }}"
                                                                    class="text-success">Manage Address</a></small>
                                                    </div>
                                                    <div class="">
                                                        {% if error_address %}
                                                            <div class="text-danger">{{ error_address }}</div>
                                                        {% endif %}
                                                        {% if home_default_address %}
                                                            <p>
                                                                {{ home_default_address.address_1 }},
                                                                {{ home_default_address.address_2 }},
                                                                {{ home_default_address.city }},
                                                                {{ home_default_address.zone }},
                                                                {{ home_default_address.country }}
                                                            </p>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label" for="input-email">{{ entry_email }}
                                                        <span class="text-danger">*</span> </label>
                                                    <div class="">
                                                        <input type="email" name="email" value="{{ email }}"
                                                               placeholder="{{ entry_email }}" id="input-email"
                                                               class="form-control rounded-0"/>
                                                        {% if error_email %}
                                                            <div class="text-danger">{{ error_email }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>

                                                <div class="col-md-6 form-group required">
                                                    <label class="control-label"
                                                           for="input-telephone">{{ entry_telephone }}<span
                                                                class="text-danger">*</span> </label>
                                                    <div class="">
                                                        <input type="tel" name="telephone" value="{{ telephone }}"
                                                               placeholder="{{ entry_telephone }}" id="input-telephone"
                                                               class="form-control rounded-0"/>
                                                        {% if error_telephone %}
                                                            <div class="text-danger">{{ error_telephone }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="row">
                                                <div class="col-md-12 form-group required image-upload mb-4">
                                                    <label class="control-label" for="input-image">Profile Photo<span
                                                                class="text-danger">*</span> </label>
                                                    <div class="upload-priview d-flex align-items-center w-100">
                                                        <div class="file-upload ">
                                                            <img src="catalog/view/theme/champion/image/upload-bg.jpg"
                                                                 alt="" class="img-fluid">
                                                            <input type="file" name="avatar"
                                                                   id="input-avatar"
                                                                   class="image"/>
                                                        </div>
                                                        <div class="img-view d-flex align-items-center justify-content-center">
                                                            <img id="blah" src="{{ image_url }}" alt="your image"
                                                                 width="140" height="140"/>
                                                        </div>
                                                        {% if error_avatar %}
                                                            <div class="text-danger">{{ error_avatar }}</div>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                            </div>

                                            {% if role == 'delivery-partner' %}
{#                                                <!-- If delivery partner -->#}
{#                                                <hr>#}
{#                                                <b>Delivery Partner Profile</b>#}
{#                                                <div class="row">#}
{#                                                    <div class="form-group">#}
{#                                                        <label class="col-sm-12 control-label"#}
{#                                                               for="input-category">{{ entry_countries }}</label>#}
{#                                                        <div class="col-sm-12">#}
{#                                                            <input type="text" name="country" value=""#}
{#                                                                   placeholder="{{ entry_country }}" id="input-country"#}
{#                                                                   class="form-control"/>#}
{#                                                            <div id="countries" class="card card-sm"#}
{#                                                                 style="height: 150px; overflow: auto;"> {% for data_country in my_countries %}#}
{#                                                                    <div id="countries{{ data_country.country_id }}">#}
{#                                                                        <i class="fa fa-minus-circle"></i> {{ data_country.name }}#}
{#                                                                        <input type="hidden" name="countries[]"#}
{#                                                                               value="{{ data_country.country_id }}"/>#}
{#                                                                    </div>#}
{#                                                                {% endfor %}</div>#}
{#                                                        </div>#}
{#                                                    </div>#}
{#                                                </div>#}
                                            {% endif %}

                                        </fieldset>
                                        <div class="buttons clearfix">
                                            <input type="submit" value="UPDATE"
                                                   class="btn btn-success w-50 custom-btn"/>

                                            <a href="{{ deactivate_link }}">Request To Deactivate Account</a>
                                        </div>
                                    </form>
                                    {{ content_bottom }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<script type="text/javascript"><!--
    // Sort the custom fields
    $('.form-group[data-sort]').detach().each(function () {
        if ($(this).attr('data-sort') >= 0 && $(this).attr('data-sort') <= $('.form-group').length) {
            $('.form-group').eq($(this).attr('data-sort')).before(this);
        }

        if ($(this).attr('data-sort') > $('.form-group').length) {
            $('.form-group:last').after(this);
        }

        if ($(this).attr('data-sort') == $('.form-group').length) {
            $('.form-group:last').after(this);
        }

        if ($(this).attr('data-sort') < -$('.form-group').length) {
            $('.form-group:first').before(this);
        }
    });
    //--></script>
<script type="text/javascript"><!--
    $('button[id^=\'button-custom-field\']').on('click', function () {
        var element = this;

        $('#form-upload').remove();

        $('body').prepend('<form enctype="multipart/form-data" id="form-upload" style="display: none;"><input type="file" name="file" /></form>');

        $('#form-upload input[name=\'file\']').trigger('click');

        if (typeof timer != 'undefined') {
            clearInterval(timer);
        }

        timer = setInterval(function () {
            if ($('#form-upload input[name=\'file\']').val() != '') {
                clearInterval(timer);

                $.ajax({
                    url: 'index.php?route=tool/upload',
                    type: 'post',
                    dataType: 'json',
                    data: new FormData($('#form-upload')[0]),
                    cache: false,
                    contentType: false,
                    processData: false,
                    beforeSend: function () {
                        $(element).button('loading');
                    },
                    complete: function () {
                        $(element).button('reset');
                    },
                    success: function (json) {
                        $(element).parent().find('.text-danger').remove();

                        if (json['error']) {
                            $(element).parent().find('input').after('<div class="text-danger">' + json['error'] + '</div>');
                        }

                        if (json['success']) {
                            alert(json['success']);

                            $(element).parent().find('input').val(json['code']);
                        }
                    },
                    error: function (xhr, ajaxOptions, thrownError) {
                        alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
                    }
                });
            }
        }, 500);
    });
    //--></script>
<script type="text/javascript"><!--
    $('.date').datetimepicker({
        language: '{{ datepicker }}',
        pickTime: false
    });

    $('.datetime').datetimepicker({
        language: '{{ datepicker }}',
        pickDate: true,
        pickTime: true
    });

    $('.time').datetimepicker({
        language: '{{ datepicker }}',
        pickDate: false
    });


    function readURL(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#blah').attr('src', e.target.result);
            }

            reader.readAsDataURL(input.files[0]);
        }
    }

    $("#input-avatar").change(function () {
        readURL(this);
    });

    // Category
    $('input[name=\'country\']').autocomplete({
        'source': function (request, response) {
            $.ajax({
                url: 'index.php?route=account/edit/country_autocomplete&filter_name=' + encodeURIComponent(request),
                dataType: 'json',
                success: function (json) {
                    response($.map(json, function (item) {
                        return {
                            label: item['name'],
                            value: item['country_id']
                        }
                    }));
                }
            });
        },
        'select': function (item) {
            $('input[name=\'country\']').val('');

            $('#countries' + item['value']).remove();

            $('#countries').append('<div id="countries' + item['value'] + '"><i class="fa fa-minus-circle"></i> ' + item['label'] + '<input type="hidden" name="countries[]" value="' + item['value'] + '" /></div>');
        }
    });

    $('#countries').delegate('.fa-minus-circle', 'click', function () {
        $(this).parent().remove();
    });
    //--></script>
{{ footer }}
